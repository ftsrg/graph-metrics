allprojects {
  group = 'graph-metrics'
  version = '0.0.1-SNAPSHOT'
}

subprojects {
  apply plugin: 'java'

  jar {
    from sourceSets.main.allJava
  }

  sourceCompatibility = 1.8
  targetCompatibility = 1.8

  repositories {
    jcenter()
  }
}

ext {
  junitVersion = '4.12'
}
